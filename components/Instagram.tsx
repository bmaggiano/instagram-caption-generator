import { FaInstagram } from 'react-icons/fa';

export default function InstagramIcon({ className, size }: { className?: string, size?: number }) {
  return <FaInstagram className={className} size={size} />;
}
